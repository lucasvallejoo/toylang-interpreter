// Brief sample 6 - classic recursive factorial.
// Base case is `n <= 0` (returns 1), so fact_rec(0) and fact_rec(1) both
// terminate cleanly without bottoming out.
// Expected output:
//   r = 120
fun fact_rec(n) { if n <= 0 then return 1 else return n * fact_rec(n - 1) }
r = fact_rec(5)
